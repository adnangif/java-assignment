# --- H2 (default, zero setup) ---
spring.datasource.url=jdbc:h2:mem:seqdb;DB_CLOSE_DELAY=-1;MODE=MySQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.sql.init.mode=always

# --- MySQL (uncomment to switch; no Java code changes needed) ---
# spring.datasource.url=jdbc:mysql://localhost:3306/seqdb
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=root
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# (also add runtime dep: com.mysql:mysql-connector-j)
